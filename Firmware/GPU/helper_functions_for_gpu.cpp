using namespace gpu;

class helper_functions_for_gpu{

    gpu_information return_gpu_information(uint64_t address_of_mount_point)
    {
        /** Explanantion of function:
         * Queries the mount point address in RAM to get information about the GPU and returns it.
         */
        gpu_information gpu_info = new gpu_information();
    }
};