namespace graph_theory{
    typedef struct map{
        // Map as a 2D matrix.
        long long int map_length;
        long long int map_breadth;
        char grid [length_of_map][breadth_of_map]; //Map grid.
    };
}